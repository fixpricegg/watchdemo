# WatchDemo analyzer

Current prototype analyzer. Run `demo_script.py` from this directory after placing a local `.dem` file under `demos/` and updating `demo_path` and `player` in the script.

Generated `radar.json`, `report.json`, and `report.md` are intentionally excluded from Git.
