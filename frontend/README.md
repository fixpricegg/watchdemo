# WatchDemo frontend

React/Vite prototype for Summary, Radar, and Timeline.

The repository contains small synthetic `radar.json` and `report.json` fixtures so the UI can start without publishing data from a real match. Replace them locally with analyzer output during development.

```bash
npm install
npm run dev
```
